---
title: "code_notes"
output: html_document
---

apply(matrix,2,mean) # to get the mean of each column.
library()
confint(model,level = 0.95) # gives the CI of all betas
cor(x,y) correlation coefficient
coef(model) - coefficients
fitted(model) - predicted y values
par(mfrow = c(x,y))
rep(1:10 , n) repeat the vector n times to generate a new vector.
sample(vector) shuffle the vector
table(vector) # return count of each element in the vector
# the following two take different types of parameters
nrow(dataframe)
length(vector)

# matrix 
A[, vector] # select columns that are related to the vector.
A[vector,] # select the rows.


# select from a table

subset(x, subset, select)

pbinom(5, size = 20, prob = 0.3) # probability with less than or equal to 5 times success for 20 trials.



